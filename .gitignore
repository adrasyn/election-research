# macOS
.DS_Store
._*

# Editors / IDEs
.vscode/
.idea/
*.swp
*~

# Environment
.env
.env.local

# Python (when we add the data pipeline)
__pycache__/
*.py[cod]
.venv/
venv/
.python-version

# Node (if any web tooling lands later)
node_modules/
npm-debug.log*

# Build outputs
dist/
build/
out/
*.zip
*.tar.gz
.astro/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# Data caches (pipeline scratch space)
.cache/
data/raw/
data/tmp/
*.parquet.tmp
pipeline/data/raw/
pipeline/data/cache/

# Pipeline raw caches (large — re-fetchable on demand)
pipeline/data/raw/

# NB: site/public/data, /seats, /tiles ARE committed — they're the
# built artefacts the deployed site serves. Cloudflare Pages doesn't
# have tippecanoe / ogr2ogr / Python in its build env, so we ship the
# pre-built outputs and rebuild locally + commit on data updates.

# Claude Code session artifacts
.claude/
